doctype html
html
  head
    title= title
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js")
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js')
    link(href='https://ajax.googleapis.com/ajax/libs/jqueryui/1.11.4/themes/redmond/jquery-ui.css', rel='stylesheet')
    script(src='https://ajax.googleapis.com/ajax/libs/jqueryui/1.11.4/jquery-ui.min.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    script.
        window.fbAsyncInit = function() {
            FB.init({
                appId      : '437339859788634',
                xfbml      : true,
                version    : 'v2.5'
            });
        FB.getLoginStatus(function(response) {if (response.status === 'connected') {var uid = response.authResponse.userID; var accessToken = response.authResponse.accessToken;
            FB.api("/154431829383/events",function (response) {if (response && !response.error) {console.log(response);createEvents(response);}});} else if (response.status === 'not_authorized') {} else {}});
        };
        function createEvents(res){var events = [];for (var i = 0; i < res.data.length; i++){events.push(res.data[i].place.name);};console.log(events);addToPage(events);};
        function addToPage(events){
            var strs = events;
            var list = document.createElement("ul");
            for (var i in strs) {
                var elem = document.createElement("li");
                elem.innerHTML = strs[i];
                list.appendChild(elem);}
            document.body.appendChild(list);};
        (function(d, s, id){
            var js, fjs = d.getElementsByTagName(s)[0];
            if (d.getElementById(id)) {return;}
            js = d.createElement(s); js.id = id;
            js.src = "//connect.facebook.net/en_US/sdk.js";
            fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));
    block content